# One process-global restart-scoped settings snapshot is shared by every
# agent-local preset instance. AgentLoop and compaction remain preset-local.
- insert:
    - id: epoch-reanchor-settings
      name: dsh-epoch-reanchor/settings
